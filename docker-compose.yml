services:
  app:
    build: .
    ports:
      - "8001:8000"
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
      - DB_HOST=wms-postgres-db.cpis084golp7.ap-south-1.rds.amazonaws.com
      - DB_PORT=5432
      - DB_NAME=warehouse_db
      - DB_USER=wmsadmin
      - DB_PASSWORD=Candorfoods
      - API_CORS_ORIGINS=*
      - JWT_SECRET=KflnDkJ4y9vVw7GkPq2nE6cL1N8bT3xR5jZmY0oS_uVhA2qCdXs
      - JWT_ALGORITHM=HS256
      - JWT_EXPIRATION_HOURS=24
      - OPENFGA_API_URL=https://api.us1.fga.dev
      - OPENFGA_STORE_ID=01K62FHS797YKEVFHAD8NMF445
      - OPENFGA_MODEL_ID=01K62XGFDFAJMBQDAX1SYAX4X9
      - OPENFGA_ENABLED=true
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      # Mount volume for application logs
      - ./logs:/app/logs
      # Mount volume for uploaded files (labels)
      - ./uploads:/app/uploads

networks:
  default:
    name: inventory-network